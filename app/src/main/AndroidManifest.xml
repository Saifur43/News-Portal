<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.saifur43.wordfrequency">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/news"
        android:label="@string/app_name"
        android:roundIcon="@drawable/news"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".News"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
        <activity
            android:name=".TopNews"
            android:label="শিরোনাম" />
        <activity
            android:name=".PostDetails"
            android:label="বিস্তারিত সংবাদ" />
        <activity
            android:name=".MainActivity"
            android:label="বিস্তারিত সংবাদ" />
        <activity
            android:name=".SearchNews"
            android:label="সংবাদ খোজ"></activity>


</application>

</manifest>